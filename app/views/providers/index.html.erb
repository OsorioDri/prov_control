<div class="container p-4">
  <h1>Provedores cadastrados </h1>
  <table class="table table-hover">
    <thead class="thead-dark">
      <tr>
        <th>Razão Social</th>
        <th>CNPJ</th>
        <th>Nome do Contato</th>
      </tr>
    </thead>
    <tbody>
      <% @providers.each do | provider| %>
        <tr>
          <td><%= provider.name %></td>
          <td><%= provider.cnpj %></td>
          <td><%= provider.contact_name %></td>
          <td><%= link_to "Editar provedor", new_restaurant_path , class: "btn btn-magic" %></td>
          <td><i class="fa-solid fa-pencil"></i></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
